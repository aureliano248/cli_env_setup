# Files Module

This module owns safe updates to user-managed files.

- `managed.sh` creates timestamped backups and replaces marked managed blocks.

Use this module whenever a feature needs to update an existing user file without taking ownership of the whole file.
