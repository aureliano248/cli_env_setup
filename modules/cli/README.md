# CLI Module

This module owns user-facing command-line behavior.

- `args.sh` prints help, parses options, validates basic prefix settings, and prints the run summary.

Keep CLI wording and option defaults here. Implementation modules should consume normalized globals.
