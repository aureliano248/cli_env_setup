# SSH Module

This module owns optional SSH public key setup.

- `authorized_keys.sh` validates accepted public key prefixes and appends a pasted key to `authorized_keys`.

Do not store private keys, tokens, or machine-specific secrets in this repository.
